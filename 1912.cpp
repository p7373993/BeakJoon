#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

vector<pair<int, int>> xy;
int n;
int num[100001];

b[n] = b[n-1] + an || an?
int back(int idx)
{

	for (size_t i = idx; i >0; i--)
	{

	}
}

		

int main()
{
	cin >> n;
	for (size_t i = 0; i < n; i++)
	{
		cin >> num[i + 1];
	}
}